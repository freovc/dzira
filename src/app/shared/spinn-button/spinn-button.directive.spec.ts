import { SpinnButtonDirective } from './spinn-button.directive';

describe('SpinnButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinnButtonDirective();
    expect(directive).toBeTruthy();
  });
});
